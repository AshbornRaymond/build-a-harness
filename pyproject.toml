[project]
name = "harness-cv"
version = "0.1.0"
description = "LLM agent harness built from scratch for benchmarking"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
